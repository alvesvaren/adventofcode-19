# Advent of code 2019 code

## config.json syntax

Make sure your config.json file follows this syntax for the automatic input downloading to work!

```json
{
    "getInput": {
        "cookies": {
            "session": "your session token from adventofcode"
        }
    }
}
```
